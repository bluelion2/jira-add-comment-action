{
  "name": "jira_add_cooment_action",
  "version": "1",
  "private": false,
  "dependencies": {
    "@actions/core": "^1.8.0",
    "@actions/github": "^5.0.1",
    "axios": "^0.24.0"
  },
  "scripts": {
    "build": "rm -rf dist && npx ncc build index.js"
  }
}